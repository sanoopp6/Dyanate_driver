<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_home"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".views.HomeActivity" >

    <Button
        android:layout_marginTop="30dp"
        android:layout_gravity="top|center_horizontal"
        android:id="@+id/statusButton"
        android:layout_width="wrap_content"
        android:layout_height="30dp"
        android:textColor="@color/blueColor"
        android:text="@string/app_name"
        android:textAllCaps="false"
        style="@style/FontSizeSixteen"
        android:background="@android:color/transparent"/>

    <FrameLayout
        android:layout_gravity="center"
        android:id="@+id/makeOnlineFrameLayout"
        android:layout_width="130dp"
        android:layout_height="133dp">

        <ImageView
            android:id="@+id/makeOnlineImageView"
            android:layout_gravity="bottom"
            android:tint="@color/redColor"
            android:padding="3dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_parking_worker"
            android:layout_width="match_parent"
            android:layout_height="100dp" />

        <TextView
            android:id="@+id/makeOnlineTextView"
            android:layout_gravity="top"
            android:textAlignment="center"
            android:layout_width="match_parent"
            style="@style/FontSizeSixteen"
            android:text="@string/MakeOnline"
            android:textAllCaps="true"
            android:textColor="@color/redColor"
            android:layout_height="33dp" />

    </FrameLayout>

</FrameLayout>
